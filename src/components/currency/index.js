import React from "react";

function Currency(props) {
  return <></>;
}
export default Currency;
{
  /* <div style={{display:'inline'}}>
    {props.currency === "USD" && "US$"}
    {props.currency === "KRW" && "₩"}
    {props.currency === "JPY" && "¥"}
    {props.currency != "USD" && props.currency != "KRW" && props.currency != "JPY" && props.currency }
    
    </div> */
}
